import React from 'react';

const page = () => {
    return (
        <div className="flex items-center justify-center h-screen">
            <h1>Explore Page</h1>
        </div>
    );
};

export default page;